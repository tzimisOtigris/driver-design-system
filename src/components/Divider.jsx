import React from 'react';

const Divider = () => {
  const styles = {
    height: '1px',
    backgroundColor: '#eceef6',
    width: '100%',
  };

  return <div style={styles} />;
};

export default Divider;
